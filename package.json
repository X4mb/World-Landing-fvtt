{
  "name": "world-landing-fvtt",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Foundry VTT module: pick a default landing scene that activates on world start.",
  "scripts": {
    "lint": "eslint .",
    "validate": "node scripts/validate-manifest.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0"
  }
}
